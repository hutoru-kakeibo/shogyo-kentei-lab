# 商業検定ラボ — 開発ガイド

> **作業を始める前に [HANDOVER.md](HANDOVER.md) を読むこと。**
> 特に「§3 未確定・プレースホルダーのコンテンツ」と「§4 ハマりどころと技術的知見」には、現状の仕様と過去に踏んだ地雷がまとまっている。

商業高校生専門のオンライン検定対策塾のLP。Next.js 16 (App Router/Turbopack) + Tailwind CSS v4。
現状はローカル開発のみで、本番デプロイはまだ行っていない。

## コマンド

```bash
npm run dev     # 開発サーバー
npm run build   # 本番ビルド
npm run start   # 本番ビルド起動
npm run lint
```

## 必ず守ること

- **文言・データの変更は `src/lib/content.ts` を編集する**。コンポーネント側にハードコードされたコピーはほぼ無い
- **変更後は `npm run lint && npm run build` を通し、ブラウザで実表示を確認してから完了とする**。ビルドが通っただけで完了報告しない
- **開発サーバーの表示がおかしい／新しいTailwindクラスが反映されない場合は、まず `.next` を削除して再起動する**（HANDOVER.md §4.1）。Turbopackのキャッシュ不整合が原因のことが多く、コード側のバグではないことが多い
- **日本語のGoogle Fontsを追加する場合、`next/font/google` の `subsets: ["japanese"]` はビルドエラーになる**。`layout.tsx` の `<head>` に手動で `<link>` を追加する方式を使う（HANDOVER.md §4.2）
- **画像を追加する場合、直リンクせず必ずダウンロードして `public/images/` に保存する**（「ちょうどいいイラスト」の利用規約で直リンク禁止。HANDOVER.md §4.4）
- 画像ファイル名は小文字で統一する（本番のLinux環境では大文字小文字が区別されるため）

## ユーザーとのやりとり

非エンジニア寄り。実装の詳細より「見た目がどう変わるか」を重視する。変更のたびにブラウザで実表示を確認し、スクリーンショット相当の説明を添えて報告する。

@AGENTS.md
